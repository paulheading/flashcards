import { selectIndex, selectWord, showFeedback, nextStep, resetGame } from './methods'

export default {
  methods : {
    selectIndex,
    selectWord,
    showFeedback,
    nextStep,
    resetGame,
  },
};
